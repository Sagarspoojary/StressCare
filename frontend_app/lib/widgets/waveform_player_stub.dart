import 'package:flutter/material.dart';

Widget buildWaveformPlayer(String audioUrl, Color primaryColor, Color surfaceColor) {
  throw UnsupportedError('Cannot create waveform player without dart:html or dart:io');
}
